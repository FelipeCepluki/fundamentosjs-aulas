class Matematica {
    static dividir(valor1, valor2) {
        return valor1 / valor2
    }
    
    static subtrair(valor1, valor2) {
        return valor1 - valor2
    }
}

// a classe que criamos na aula de módulos nodejs funcionaria aqui, só tem uma unica excessão: esse arquivo já fica público sozinho !
// só adicionar no HTML, e você não precisa do modulo.export